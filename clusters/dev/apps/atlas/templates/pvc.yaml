apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: atlas-storage
  namespace: utility
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 5Gi
