apiVersion: v1
kind: Service
metadata:
  name: atlas
  namespace: utility
  labels:
    app.kubernetes.io/name: atlas
spec:
  type: ClusterIP
  ports:
    - port: 21000
      protocol: TCP
      targetPort: 21000
      name: "21000"
  selector:
    app.kubernetes.io/name: atlas
    app.kubernetes.io/instance: {{ .Release.Name }}
