apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: arc-runners-public
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  ignoreDifferences:
    - group: external-secrets.io
      kind: ExternalSecret
      jsonPointers:
        - /spec/dataFrom/0/extract/conversionStrategy
        - /spec/dataFrom/0/extract/decodingStrategy
        - /spec/dataFrom/0/extract/metadataPolicy
  project: default
  source:
    repoURL: https://github.com/PilotDataPlatform/pilot-hdc-platform-gitops.git
    path: clusters/dev/apps/arc-runners-public
    targetRevision: main
    helm:
      valueFiles:
        - ../../registry.yaml
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: arc-runners
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
