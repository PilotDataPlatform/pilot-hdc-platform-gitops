apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: upload-greenroom
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  project: default
  source:
    repoURL: https://github.com/PilotDataPlatform/pilot-hdc-platform-gitops.git
    targetRevision: main
    path: clusters/dev/apps/upload-greenroom
    helm:
      valueFiles:
        - ../../registry.yaml
        - ../../versions.yaml
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: greenroom
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
